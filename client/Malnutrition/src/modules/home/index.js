import React from 'react';
import {Text, SafeAreaView} from 'react-native';

const Home = () => {
  return (
    <SafeAreaView>
      <Text>Home Screen</Text>
    </SafeAreaView>
  );
};

export default Home;
