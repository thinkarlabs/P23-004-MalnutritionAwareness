import {StyleSheet} from 'react-native';

export const appVideoPlayerStyles = StyleSheet.create({
  container: {
    paddingTop: 12,
    paddingBottom: 20,
  },
  viewStyle: {
    borderRadius: 12,
  },
});
